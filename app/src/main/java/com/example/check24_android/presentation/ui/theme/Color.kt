package com.example.check24_android.presentation.ui.theme

import androidx.compose.ui.graphics.Color

// --- Main Brand Colors ---
// The primary blue used in the Header and Logo
val BrandBlue = Color(0xFF005EA8)
val BrandBlueDark = Color(0xFF00447B)


val AppBackground = Color(0xFFF5F7FA)

val BrandWhite = Color(0xFFFFFFFF)

// --- Typography ---
val TextPrimary = Color(0xFF333333)    // Main Headings (Black-ish)
val TextSecondary = Color(0xFF666666)  // Descriptions (Dark Gray)
val TextTertiary = Color(0xFF999999)   // Placeholders / Subtle text
val TextDark = Color(0xFF212121)

// --- Widget Gradients ---
// Matches the "Internet Deals" widget (Blue Gradient)
val WidgetGradientBlueStart = Color(0xFF005EA8)
val WidgetGradientBlueEnd = Color(0xFF003C71)

// Matches "Promo" or "Green" variations if needed
val WidgetGradientGreenStart = Color(0xFF43A047)
val WidgetGradientGreenEnd = Color(0xFF2E7D32)

// --- Structural Colors ---
val DividerColor = Color(0xFFE0E0E0)      // Subtle separators
val CardBorder = Color(0xFFF0F0F0)        // Optional card border

// --- Semantic & Functional ---
val StatusActive = Color(0xFF28A745)      // "Active" contracts (Vibrant Green)
val StatusWarning = Color(0xFFFFC107)     // "Expires Soon" (Amber)
val PriceHighlight = Color(0xFFD32F2F)    // Discount prices (Red)
val ErrorColor = Color(0xFFB00020)

// --- Navigation Bar ---
val NavSelected = Color(0xFF005EA8)       // Matches Brand Blue
val NavUnselected = Color(0xFF9E9E9E)     // Greyed out icons

// --- Buttons ---
val ButtonPrimary = Color(0xFF005EA8)
val ButtonPrimaryPressed = Color(0xFF004680)


val ButtonSecondary = Color(0xFF66BB6A)

val LightGray = Color(0xFFF5F5F5)
